# Copy to .env (or export in your shell) — never commit real values.

# Notion integration secret from https://www.notion.so/my-integrations
NOTION_API_KEY=

# ID of the Notion page the 5 databases get created under (share the page
# with your integration first, or database creation will 404).
NOTION_PARENT_PAGE_ID=
